{
  "name": "ghost-editor-adapter",
  "version": "0.1.0",
  "private": true,
  "description": "Drop-in open-source browser image editor for self-hosted Ghost using Ghost's configurable image-editor loading seam.",
  "type": "module",
  "scripts": {
    "check": "node --check dist/ghost-editor-adapter.js && node --check dist/editor.js && node --check dist/editor-ui.js && node --test tests/*.test.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
